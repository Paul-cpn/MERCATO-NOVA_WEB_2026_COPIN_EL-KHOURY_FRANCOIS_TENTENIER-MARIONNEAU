const { useState } = React;

function App() {
  return (
    <div>
      <h1>Mercato Nova</h1>
    </div>
  );
}

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
